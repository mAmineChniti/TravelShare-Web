<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8" />
		<title>Reçu VIP - {{ excursion.title }}</title>
		<style>
			@import url("https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;700;900&family=Roboto:wght@300;400;500&display=swap");

			:root {
				--gold: #d4af37;
				--dark-blue: #0a2463;
				--cream: #f5f0e5;
				--light-gold: #f8e8c8;
				--black: #1a1a1a;
			}

			body {
				font-family: "Roboto", sans-serif;
				margin: 0;
				padding: 0;
				background-color: var(--cream);
				color: var(--black);
				min-height: 100vh;
				display: flex;
				justify-content: center;
				align-items: center;
			}

			.container {
				width: 90%;
				max-width: 850px;
				margin: 40px auto;
				background: white;
				box-shadow: 0 30px 60px -20px rgba(0, 0, 0, 0.2);
				border-radius: 2px;
				overflow: hidden;
				position: relative;
				border: 1px solid var(--light-gold);
			}

			.gold-border {
				position: absolute;
				top: 0;
				left: 0;
				width: 100%;
				height: 8px;
				background: linear-gradient(90deg, var(--gold), #f5d07c, var(--gold));
				z-index: 10;
			}

			.header {
				background: var(--dark-blue);
				color: white;
				padding: 50px 40px 40px;
				text-align: center;
				position: relative;
				overflow: hidden;
			}

			.header::before {
				content: "";
				position: absolute;
				top: -100px;
				right: -100px;
				width: 300px;
				height: 300px;
				background: radial-gradient(circle, rgba(212, 175, 55, 0.1) 0%, rgba(212, 175, 55, 0) 70%);
				border-radius: 50%;
			}

			.logo {
				font-family: "Playfair Display", serif;
				font-size: 42px;
				font-weight: 900;
				margin-bottom: 10px;
				letter-spacing: 1px;
				color: var(--gold);
				position: relative;
				display: inline-block;
			}

			.logo::after {
				content: "";
				position: absolute;
				bottom: -10px;
				left: 50%;
				transform: translateX(-50%);
				width: 80px;
				height: 3px;
				background: var(--gold);
			}

			.subtitle {
				font-weight: 300;
				letter-spacing: 4px;
				text-transform: uppercase;
				font-size: 12px;
				margin-top: 15px;
				color: rgba(255, 255, 255, 0.8);
			}

			.content {
				padding: 50px;
				position: relative;
			}

			.receipt-title {
				font-family: "Playfair Display", serif;
				font-size: 32px;
				font-weight: 700;
				color: var(--dark-blue);
				margin-bottom: 40px;
				text-align: center;
				position: relative;
			}

			.receipt-title::after {
				content: "";
				position: absolute;
				bottom: -15px;
				left: 50%;
				transform: translateX(-50%);
				width: 100px;
				height: 2px;
				background: var(--gold);
			}

			.details-container {
				display: flex;
				flex-wrap: wrap;
				gap: 30px;
				margin-bottom: 40px;
				align-items: flex-start;
			}

			.detail-card {
				flex: 1;
				min-width: 250px;
				background: white;
				border-radius: 2px;
				padding: 25px;
				box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
				border-top: 4px solid var(--gold);
				position: relative;
				overflow: hidden;
			}

			.experience-card {
				margin-top: 30px;
				position: relative;
			}

			.experience-card::before {
				content: "";
				position: absolute;
				top: -30px;
				left: 0;
				right: 0;
				height: 30px;
				background: transparent;
			}

			.detail-card::before {
				content: "";
				position: absolute;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				background: linear-gradient(135deg, rgba(212, 175, 55, 0.03) 0%, rgba(212, 175, 55, 0) 100%);
				z-index: 0;
			}

			.detail-card-title {
				font-family: "Playfair Display", serif;
				font-size: 18px;
				font-weight: 700;
				color: var(--dark-blue);
				margin-bottom: 20px;
				position: relative;
				display: flex;
				align-items: center;
			}

			.detail-card-title svg {
				margin-right: 10px;
				width: 20px;
				height: 20px;
				fill: var(--gold);
			}

			.detail-row {
				display: flex;
				justify-content: space-between;
				padding: 12px 0;
				border-bottom: 1px solid rgba(0, 0, 0, 0.05);
			}

			.detail-row:last-child {
				border-bottom: none;
			}

			.experience-item {
				display: flex;
				justify-content: space-between;
				padding: 12px 0;
				border-bottom: 1px solid rgba(0, 0, 0, 0.05);
			}

			.experience-item:last-child {
				border-bottom: none;
			}

			.detail-label,
			.experience-label {
				font-weight: 400;
				color: #666;
				font-size: 14px;
			}

			.detail-value {
				font-weight: 500;
				color: var(--black);
				text-align: right;
			}

			.price {
				font-family: "Playfair Display", serif;
				font-size: 28px;
				color: var(--gold);
				font-weight: 700;
			}

			.vip-badge {
				position: absolute;
				top: 20px;
				right: 20px;
				background: var(--gold);
				color: var(--dark-blue);
				padding: 5px 15px;
				border-radius: 20px;
				font-size: 12px;
				font-weight: 700;
				letter-spacing: 1px;
			}

			.included-badge {
				background-color: rgba(212, 175, 55, 0.1);
				color: var(--gold);
				padding: 4px 10px;
				border-radius: 12px;
				font-size: 12px;
				font-weight: 500;
			}

			.footer {
				text-align: center;
				padding: 30px;
				color: #666;
				font-size: 13px;
				border-top: 1px solid rgba(0, 0, 0, 0.05);
				position: relative;
			}

			.footer::before {
				content: "";
				position: absolute;
				top: -1px;
				left: 50%;
				transform: translateX(-50%);
				width: 200px;
				height: 1px;
				background: var(--gold);
			}

			.signature {
				margin-top: 40px;
				text-align: right;
			}

			.signature-line {
				width: 200px;
				height: 1px;
				background: var(--gold);
				margin: 5px 0 10px;
				display: inline-block;
			}

			.signature-name {
				font-family: "Playfair Display", serif;
				color: var(--dark-blue);
				font-size: 18px;
			}

			.gold-text {
				color: var(--gold);
			}

			.confirmation-stamp {
				position: absolute;
				top: -30px;
				right: 30px;
				width: 120px;
				height: 120px;
				border: 5px solid var(--gold);
				border-radius: 50%;
				display: flex;
				align-items: center;
				justify-content: center;
				background: white;
				transform: rotate(15deg);
				box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
				z-index: 2;
			}

			.confirmation-stamp::before {
				content: "CONFIRMÉ";
				font-family: "Playfair Display", serif;
				font-weight: 700;
				color: var(--gold);
				font-size: 16px;
				letter-spacing: 2px;
			}

			.description-value {
				font-weight: 500;
				color: var(--black);
				text-align: left;
				margin-top: 5px;
				font-size: 13px;
				line-height: 1.5;
			}
		</style>
	</head>
	<body>
		<div class="container">
			<div class="gold-border"></div>

			<div class="header">
				<div class="logo">TRAVELASHARE</div>
				<div class="subtitle">LUXURY TRAVEL EXPERIENCE</div>
			</div>

			<div class="content">
				<div class="confirmation-stamp"></div>

				<div class="receipt-title">Reservation Confirmation</div>

				<div class="details-container">
					<div class="detail-card">
						<div class="vip-badge">VIP ACCESS</div>
						<div class="detail-card-title">
							<svg viewBox="0 0 24 24">
								<path
									d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8z"
								/>
								<path
									d="M12 6c-3.31 0-6 2.69-6 6s2.69 6 6 6 6-2.69 6-6-2.69-6-6-6zm0 10c-2.21 0-4-1.79-4-4s1.79-4 4-4 4 1.79 4 4-1.79 4-4 4z"
								/>
							</svg>
							Reservation Details
						</div>
						<div class="detail-row">
							<span class="detail-label">Reference Number</span>
							<span class="detail-value">#{{ excursion.excursionId|upper }}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Date of Issue</span>
							<span class="detail-value">{{ "now"|date('d M Y') }}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Status</span>
							<span class="detail-value gold-text">Confirmed</span>
						</div>
						<div class="detail-row" style="flex-direction: column; align-items: flex-start">
							<span class="detail-label">Excursion</span>
							<span class="detail-value">{{ excursion.title }}</span>
						</div>
						<div class="detail-row" style="flex-direction: column; align-items: flex-start">
							<span class="detail-label">Description</span>
							<span class="description-value">{{ excursion.description }}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Date</span>
							<span class="detail-value">{{ excursion.dateExcursion|date('d F Y') }}</span>
						</div>
					</div>

					<div class="detail-card">
						<div class="detail-card-title">
							<svg viewBox="0 0 24 24">
								<path
									d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8zm.31-8.86c-1.77-.45-2.34-.94-2.34-1.67 0-.84.79-1.43 2.1-1.43 1.38 0 1.9.66 1.94 1.64h1.71c-.05-1.34-.87-2.57-2.49-2.97V5H10.9v1.69c-1.51.32-2.72 1.3-2.72 2.81 0 1.79 1.49 2.69 3.66 3.21 1.95.46 2.34 1.15 2.34 1.87 0 .53-.39 1.39-2.1 1.39-1.6 0-2.23-.72-2.32-1.64H8.04c.1 1.7 1.36 2.66 2.86 2.97V19h2.34v-1.67c1.52-.29 2.72-1.16 2.73-2.77-.01-2.2-1.9-2.96-3.66-3.42z"
								/>
							</svg>
							Payment Summary
						</div>
						<div class="detail-row">
							<span class="detail-label">Subtotal</span>
							<span class="detail-value">{{ excursion.prix }} TND</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Taxes & Fees</span>
							<span class="detail-value">0 TND</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Total Amount</span>
							<span class="detail-value price">{{ excursion.prix }} TND</span>
						</div>
					</div>
				</div>

				<div class="signature">
					<div>With gratitude,</div>
					<div class="signature-line"></div>
					<div class="signature-name">The TravelaShare Team</div>
				</div>
			</div>

			<div class="footer">
				<p>For any inquiries, please contact our concierge service</p>
				<p><span class="gold-text">contact@travelashare.com</span> • +216 52 751 485</p>
				<p style="margin-top: 15px; font-size: 11px; letter-spacing: 1px">
					TRAVELASHARE © {{ "now"|date('Y') }} | ALL RIGHTS RESERVED
				</p>
			</div>
		</div>
	</body>
</html>
